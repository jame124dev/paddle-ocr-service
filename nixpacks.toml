[phases.setup]
aptPkgs = [
  "libgomp1",
  "libglib2.0-0",
  "libsm6",
  "libxrender1",
  "libxext6",
  "libgl1"
]

[start]
cmd = "gunicorn -w 1 -b 0.0.0.0:${PORT:-8080} --timeout 300 ocr_service:app"
